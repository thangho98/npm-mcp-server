# Nginx Proxy Manager MCP Server Configuration

# URL of Nginx Proxy Manager (including port, usually 81)
NPM_URL=http://localhost:81

# NPM login email (admin email)
NPM_EMAIL=admin@example.com

# NPM login password
NPM_PASSWORD=changeme

# Readonly mode - blocks create/update/delete operations
# Set to "true" to only allow viewing information without modifying configuration
NPM_READONLY=false
